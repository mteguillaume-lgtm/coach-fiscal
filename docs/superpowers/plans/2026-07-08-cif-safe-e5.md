# Reformulation CIF-safe des leviers (audit E5) — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Les 10 leviers « décision d'investissement » reformulés en pédagogie conditionnelle (« À étudier : … », chiffrage conservé), mention non-conseil centralisée sur le panneau d'opportunités, noms de banques retirés, seuils RFR LEP sortis du code (paperasse-first), et un verrou de test qui empêche tout futur levier de recommencer.

**Architecture:** Spec (= livrable avocat) : `docs/superpowers/specs/2026-07-08-cif-safe-e5-design.md` — la table avant/après y fait foi, le plan l'applique littéralement. Constante `MENTION_NON_CONSEIL` dans conseilPatrimonial.js, rendue par `OpportunitiesPanel`. Verrou statique `cif-safe.test.js` sur le début des chaînes `action:`.

**Tech Stack:** Vitest ; textes appliqués par substitutions exactes (python heredoc si chaînes longues).

## Global Constraints

- **La table avant/après de la spec fait foi** — appliquer les textes « Après » à l'identique.
- Impératifs déclaratifs/routage **inchangés** (Reporter, Déclarer, Vérifier, Conserver, Consulter, Rédiger, Augmenter, Comparer, Identifier, Concentrer).
- `questionChat`, `titre`, `description` inchangés (sauf retrait noms de banques si présents en description LEP — vérifier).
- Les tests `arbitrage-2op.test.js` matchent `/PFU/` et `/barème/` sur titre+action → les reformulations 2OP doivent conserver ces mots (c'est le cas dans la spec).
- Suite verte à chaque commit (744 au départ). Branche : `cif-safe-e5`. Commits `feat(e5): …`.

---

### Task 1: Seuils RFR LEP depuis le JSON (paperasse-first)

**Files:**
- Modify: `src/data/epargne-reglementee.json` (bloc `lep`)
- Modify: `src/lib/taxCalculator.js` (2 exports sous PLAFOND_LEP)
- Modify: `src/lib/opportunitiesDetector.js` (~ligne 539 : `const plafondRFR = isPacseOuMarie ? 34_393 : 22_419;`)
- Modify: `src/lib/__tests__/paperasse-first.test.js` (nouveau motif interdit)
- Test: `src/lib/__tests__/constantes-centralisees.test.js` (étendre)

- [ ] **Step 0:** `git checkout -b cif-safe-e5`

- [ ] **Step 1: Test qui échoue** — dans `constantes-centralisees.test.js`, étendre l'import taxCalculator avec `SEUIL_RFR_LEP_SOLO, SEUIL_RFR_LEP_COUPLE` et ajouter :

```js
  it('seuils RFR d\'éligibilité LEP lus depuis epargne-reglementee.json (audit E5)', () => {
    expect(SEUIL_RFR_LEP_SOLO).toBe(22_419);
    expect(SEUIL_RFR_LEP_COUPLE).toBe(34_393);
  });
```

Run → FAIL (exports absents).

- [ ] **Step 2: JSON** — dans `epargne-reglementee.json`, remplacer :

```json
  "lep":      { "plafond_versements": 10000 }
```

par :

```json
  "lep": {
    "plafond_versements": 10000,
    "seuil_rfr_celibataire": 22419,
    "seuil_rfr_couple_2_parts": 34393,
    "_source_seuils": "Éligibilité LEP RFR N-2, valeurs revenus 2025 (2 parts pour un couple) — art. L221-15 CMF / service-public F2367"
  }
```

- [ ] **Step 3: Exports** — dans `taxCalculator.js`, sous `PLAFOND_LEP` :

```js
export const SEUIL_RFR_LEP_SOLO   = epargneRegRaw.lep.seuil_rfr_celibataire;
export const SEUIL_RFR_LEP_COUPLE = epargneRegRaw.lep.seuil_rfr_couple_2_parts;
```

- [ ] **Step 4: Détecteur** — étendre l'import taxCalculator du détecteur avec les 2 constantes, puis remplacer :

```js
    const plafondRFR = isPacseOuMarie ? 34_393 : 22_419;
```

par :

```js
    const plafondRFR = isPacseOuMarie ? SEUIL_RFR_LEP_COUPLE : SEUIL_RFR_LEP_SOLO;
```

- [ ] **Step 5: Verrou** — dans `paperasse-first.test.js`, ajouter au tableau `FORBIDDEN` :

```js
  { re: /\b(?:34_?393|22_?419)\b/,           why: 'seuils RFR LEP → SEUIL_RFR_LEP_* (epargne-reglementee.json)' },
```

- [ ] **Step 6:** `npx vitest run` → tout vert. Commit :

```bash
git add src/data/epargne-reglementee.json src/lib/taxCalculator.js src/lib/opportunitiesDetector.js src/lib/__tests__/paperasse-first.test.js src/lib/__tests__/constantes-centralisees.test.js
git commit -m "fix(paperasse): seuils RFR LEP lus depuis epargne-reglementee.json (audit E5)"
```

---

### Task 2: Verrou CIF-safe (test) — écrit AVANT les reformulations, il doit échouer

**Files:**
- Test: `src/lib/__tests__/cif-safe.test.js` (create)

- [ ] **Step 1: Écrire le verrou** :

```js
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { MENTION_NON_CONSEIL } from '../conseilPatrimonial';

// Verrou E5 : aucun levier ne doit COMMENCER son champ `action` par un verbe
// d'ordre décisionnel d'investissement (zone CIF/AMF). Les impératifs
// déclaratifs/administratifs et le routage professionnel restent autorisés :
// Reporter, Déclarer, Vérifier, Conserver, Consulter, Rédiger, Augmenter,
// Comparer, Identifier, Concentrer.
const SRC = readFileSync(
  fileURLToPath(new URL('../opportunitiesDetector.js', import.meta.url)), 'utf8',
);

describe('CIF-safe — pédagogie conditionnelle, pas d\'ordre décisionnel (audit E5)', () => {
  it('aucune chaîne action ne commence par un verbe décisionnel d\'investissement', () => {
    const lignes = SRC.split('\n');
    const fautes = [];
    for (let i = 0; i < lignes.length; i++) {
      if (!/^\s*action:/.test(lignes[i])) continue;
      // Bloc action = la ligne + ses continuations ternaires immédiates (? '…' / : '…').
      const bloc = [lignes[i]];
      let j = i + 1;
      while (j < lignes.length && /^\s*[?:]\s*['`]/.test(lignes[j])) bloc.push(lignes[j++]);
      for (const l of bloc) {
        if (/['`](Verser|Cocher|Ouvrir|Clôturer|Saturer|Investir|Basculer|Arbitrer|Étaler|Anticiper)\b/.test(l)) {
          fautes.push(`ligne ${i + 1}: ${l.trim().slice(0, 90)}`);
        }
      }
    }
    expect(fautes, `Verbe décisionnel en tête d'action (reformuler « À étudier : … ») :\n${fautes.join('\n')}`).toEqual([]);
  });

  it('la mention non-conseil centralisée existe', () => {
    expect(MENTION_NON_CONSEIL).toMatch(/pas un conseil/i);
  });
});
```

- [ ] **Step 2:** Run → FAIL (verbes présents + MENTION_NON_CONSEIL absente). NE PAS committer seul — le commit vient avec la Tâche 3 (verrou + mise en conformité ensemble).

---

### Task 3: Reformulations (table de la spec) + mention centralisée

**Files:**
- Modify: `src/lib/opportunitiesDetector.js` (les 10 champs `action` de la table avant/après)
- Modify: `src/lib/conseilPatrimonial.js` (constante `MENTION_NON_CONSEIL`)
- Modify: `src/components/OpportunitiesPanel.jsx` (rendu de la mention en tête)

- [ ] **Step 1: Constante** — dans `conseilPatrimonial.js`, sous `DISCLAIMER_GLOBAL` :

```js
export const MENTION_NON_CONSEIL =
  'Pistes pédagogiques chiffrées à partir de vos données — pas un conseil en '
  + 'investissement personnalisé. Chaque décision est à valider selon votre '
  + 'situation, le cas échéant avec un professionnel.';
```

- [ ] **Step 2: Appliquer la table avant/après** — pour CHAQUE entrée 1-10 de la spec
`docs/superpowers/specs/2026-07-08-cif-safe-e5-design.md`, remplacer la chaîne « Avant » par la
chaîne « Après » dans `opportunitiesDetector.js` (substitutions exactes ; pour le levier 7,
préfixer la liste : `action: plan.length > 0 ? 'À étudier (ordre indicatif) : ' + plan.map(…)`
et passer `Saturer/Ouvrir/Renforcer` des items du plan en minuscules `saturer/ouvrir/renforcer`).
Vérifier aussi la `description` du levier LEP : retirer tout nom d'établissement s'il y en a.

- [ ] **Step 3: Mention dans le panneau** — ouvrir `src/components/OpportunitiesPanel.jsx`
(`sed -n 1,40p`), importer `MENTION_NON_CONSEIL` depuis `../lib/conseilPatrimonial`, et rendre
sous le titre du panneau :

```jsx
      <p className="text-2xs text-ink-300 leading-relaxed mb-3">{MENTION_NON_CONSEIL}</p>
```

(adapter la classe au style local si le panneau est en thème clair — reprendre la palette du fichier).

- [ ] **Step 4: Vérifier** — `npx vitest run src/lib/__tests__/cif-safe.test.js` → PASS ;
`npx vitest run` → tout vert (dont `arbitrage-2op.test.js` : les mots PFU/barème sont conservés) ;
`npm run lint` → 0 erreur.

- [ ] **Step 5: Commit**

```bash
git add src/lib/opportunitiesDetector.js src/lib/conseilPatrimonial.js src/components/OpportunitiesPanel.jsx src/lib/__tests__/cif-safe.test.js
git commit -m "feat(e5): leviers en pédagogie conditionnelle + mention non-conseil + verrou CIF-safe"
```

---

### Task 4: Traçabilité + vérification finale

- [ ] **Step 1:** `docs/audit-2026-07-complet.md`, ligne E5 : `| à planifier |` → `| ✅ {date} (branche cif-safe-e5) — spec = livrable avocat |`.
- [ ] **Step 2:** `npx vitest run` + `npm run lint` + `npm run build` → verts.
- [ ] **Step 3:** Commit docs, clôture via finishing-a-development-branch (merge main + push selon le choix établi), mémoire projet mise à jour.
